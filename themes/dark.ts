import { Theme } from './index';

const dark: Theme = {
    colors: {
        primary: '#fff',
    },
    bgColors: {
        primary: 'rgba(0,0,0,0.8)',
    },
    borderColors: {
        primary: '#ccc',
    },
};

export default dark;
