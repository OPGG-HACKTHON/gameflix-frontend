import { Theme } from '@emotion/react';

const dark: Theme = {
    colors: {
        primary: '#fff',
        default: '#ccc',
    },
    buttonColors: {
        primary: 'rgba(0,0,0,0.8)',
    },
    bgColors: {
        primary: '#000',
    },
    borderColors: {
        primary: '#ccc',
    },
};

export default dark;
